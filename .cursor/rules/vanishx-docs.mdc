---
description: VanishX product docs live in sibling project-note-cursor repo
alwaysApply: true
---

# VanishX documentation source

When implementing or discussing **VanishX** in this monorepo:

1. Product docs are NOT in this repo. Read from sibling path:
   - `../project-note-cursor/vanishx-docs/`
   - Absolute: `/Users/loctra/Workspace/01_Projects/Active/project-note-cursor/vanishx-docs/`
2. Start with `vanishx-engine-vi.md` (PRD) and the **active** story under `stories/` (**Epic R** Release). **Epic 6** UX residual = **Done** → `stories/completed/6.*`. **Epic 11** Media = **Done** → `stories/completed/11.*`. **Epic 12** Chat Detail = **Done** → `stories/completed/12.*`. **Epic 13** Conversation surfaces = **Done** → `stories/completed/13.*`. **Epic 14** Deep link = **Done** → `stories/completed/14.*`. **Epic 15** Notification Center = **Done** → `stories/completed/15.*`. **Epic 16** Media capture = **Done** → `stories/16.*` (QA `tmp/epic-16-qa.md`; archive to `completed/16.*` when convenient). Archive: `stories/completed/` (Epic 1–9, **11**, **12**, **13**, **14**, **15**).
3. Core MVP + Epic 5–**6** + **Epic 11**–**16** đã ship. Phase hiện tại: **Epic R** Release Prod (R.1 → R.2 → R.4 → R.3).
4. Defer unless asked: IAP/RevenueCat SKU map, WebRTC, KMP/iOS · Caption media (E11-5) · History/Paywall deep (🅿️). App Links HTTPS = Epic 14 **Done**. FCM preview last-message = ngoài Epic 15. Prod Functions = **R.1**. Business / cost / competitive / Pro price: `vanishx-docs/business/` (snapshot 2026-08).
5. New app module: `apps/vanishx` with `applicationId` `com.vault.vanishx`. Do not put VanishX features in `core/*`.
6. See `docs/VANISHX.md` for how humans/agents should attach docs in Cursor.
7. Update product docs in `project-note-cursor`; only update this repo’s pointer files if paths/process change.
